import pytest

import FoSpy.parsing.validators.numbers


@pytest.mark.parametrize(
    "label,value_key,require_unit,expected",
    [
        # TODO: fill in test data for test_positive_decimal
        # pytest.param(, , , id="")
    ]
)
def test_positive_decimal(label, value_key, require_unit, expected):
    # TODO: create assertions for test_positive_decimal
    # FoSpy.parsing.validators.numbers.positive_decimal(label, value_key, require_unit)
    pass


@pytest.mark.parametrize(
    "label,value_key,lower,upper,include_lower,include_upper,require_unit,expected",
    [
        # TODO: fill in test data for test_decimal_range
        # pytest.param(, , , , , , , id="")
    ]
)
def test_decimal_range(label, value_key, lower, upper, include_lower, include_upper, require_unit, expected):
    # TODO: create assertions for test_decimal_range
    # FoSpy.parsing.validators.numbers.decimal_range(label, value_key, lower, upper, include_lower, include_upper, require_unit)
    pass


@pytest.mark.parametrize(
    "label,value_key,require_unit,expected",
    [
        # TODO: fill in test data for test_any_decimal
        # pytest.param(, , , id="")
    ]
)
def test_any_decimal(label, value_key, require_unit, expected):
    # TODO: create assertions for test_any_decimal
    # FoSpy.parsing.validators.numbers.any_decimal(label, value_key, require_unit)
    pass